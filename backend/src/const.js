//const.js

// Cosmos DB の設定

const AdminUser = "admin@admin.com";

const Port = 5000; // APIサーバーのポート番号

const NotFound = 0;
const PublicChange = 1; //public⇒privateに変更

module.exports = {
  TemplateDatabaseId,
  TemplateContainerId,
  FavoriteDatabaseId,
  FavoriteContainerId,
  Endpoint,
  Key,
  AdminUser,
  Port,
  NotFound,
  PublicChange,
};
